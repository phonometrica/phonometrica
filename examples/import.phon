# Import the file "utils.phon" in the same directory
import utils

# An alternative syntax is as follows. 
#   var utils = require("utils") 
# It is longer but is more flexible since 
# we can import the module under a different name.

print(utils.Pi)
utils.greet()



